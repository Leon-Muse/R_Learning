#Initializes the datasets
library(datasets)

#Gives info about the available datasets
?datasets
library(help = "datasets")

#This means we are now using the mtcars dataset
head(mtcars)

#Gives info over the datasets
?mtcars

#This is all in base Rcd 
plot(x = mtcars$qsec, y = mtcars$mpg,
     col = "red",)














